const host: string = import.meta.env.VITE_API_BASE_URL;
const auth = "auth"
const register = "register"
const login = "login"
const logout = "logout"
const secret = "secret"
const user = "user"
const admin = "admin"

export {host, auth, register, login, logout, secret, user, admin}